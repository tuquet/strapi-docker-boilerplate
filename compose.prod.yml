# =============================================================================
# Production Docker Compose - Chạy trên VPS
# Chỉ PULL images từ Registry, KHÔNG build tại VPS
#
# Setup lần đầu:
#   1. Copy project lên VPS
#   2. bash scripts/copy-env.sh --env prod    (tự set COMPOSE_FILE=compose.prod.yml)
#   3. docker login <REGISTRY_IP>:5000
#   4. docker compose up -d                   (không cần -f nữa!)
#
# Cập nhật version mới:
#   1. Sửa tag IMAGE_TAG trong .env (vd: latest -> v2)
#   2. docker compose pull
#   3. docker compose up -d
# =============================================================================

services:
  # ============================================
  # Strapi CMS - Backend API
  # Pull từ Private Registry (KHÔNG build)
  # ============================================
  strapi:
    image: ${REGISTRY_URL:-localhost:5000}/strapi-app:${IMAGE_TAG:-latest}
    container_name: strapi
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_CLIENT: ${DATABASE_CLIENT}
      DATABASE_HOST: launchpad-db
      # Override cổng kết nối nội bộ Docker (luôn là 5432) để tránh lấy nhầm port Host ở file .env
      DATABASE_PORT: 5432
      DATABASE_NAME: ${DATABASE_NAME}
      DATABASE_USERNAME: ${DATABASE_USERNAME}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      ADMIN_JWT_SECRET: ${ADMIN_JWT_SECRET}
      APP_KEYS: ${APP_KEYS}
      NODE_ENV: production
    ports:
      - '1337:1337'
    volumes:
      - strapi-uploads:/opt/app/public
    depends_on:
      launchpad-db:
        condition: service_healthy

  # ============================================
  # PostgreSQL Database
  # ============================================
  launchpad-db:
    container_name: launchpad-db
    image: postgres:16-alpine
    platform: linux/amd64
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    ports:
      - '54321:5432'
    volumes:
      - strapi-data:/var/lib/postgresql/data
    healthcheck:
      test:
        ['CMD-SHELL', 'pg_isready -U ${DATABASE_USERNAME} -d ${DATABASE_NAME}']
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # Next.js Frontend
  # Pull từ Private Registry (KHÔNG build)
  # ============================================
  nextjs:
    image: ${REGISTRY_URL:-localhost:5000}/next-app:${IMAGE_TAG:-latest}
    container_name: nextjs
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: http://strapi:1337
      NODE_ENV: production
    ports:
      - '3000:3000'
    depends_on:
      - strapi

  # ============================================
  # Nginx Reverse Proxy
  # ============================================
  nginx:
    container_name: nginx
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - '8000:80'
      - '8443:443'
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - nextjs
      - strapi

volumes:
  strapi-data:
  strapi-uploads:
